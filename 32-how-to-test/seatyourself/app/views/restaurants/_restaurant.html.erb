<section class="restaurant">
  <%= link_to restaurant.name, restaurant %>
  <% if restaurant.category %>
    <p class="category">Category: <%= restaurant.category.name %></p>
  <% end %>
  <p class="desc"><%= shorten(restaurant.description) %></p>
</section>